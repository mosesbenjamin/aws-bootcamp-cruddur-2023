#! /usr/bin/bash
-e # stop if it fails at any point

CYAN='\033[1;36m'
NO_COLOR='\033[0m'
LABEL="db-setup"
printf "${CYAN}== ${LABEL}${NO_COLOR}\n"

bin_path="$(realpath .)/bin"

source "$bin_path/rds/drop"
source "$bin_path/rds/create"
source "$bin_path/rds/schema-load"
source "$bin_path/rds/seed"